const os = require('os');
const { allowedNodeEnvironmentFlags, features } = require('process');

//here is the how we can calculate the total memory in node.js
var tm = os.totalmem();
var fm = os.freemem();

console.log('Total Memory:' + tm);

frekahfewhufuahhae

FileSystemFileHandleeaf
allowedNodeEnvironmentFlagsafa
featuresfea
f testing revert commit